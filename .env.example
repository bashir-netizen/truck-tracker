# Copy to .env and fill in. .env is gitignored — never commit the real token.

# Wialon Hosting API token (read access to the unit + its reports).
WIALON_TOKEN=

# Optional: override the SQLite path (defaults to data/truck.db).
# TRUCK_DB=data/truck.db
